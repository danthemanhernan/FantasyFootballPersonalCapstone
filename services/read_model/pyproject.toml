[project]
name = "fantasy-hud-read-model"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn>=0.30,<1",
  "asyncpg>=0.29,<1",
  "redis>=5,<7",
]

[dependency-groups]
test = ["httpx>=0.27,<1", "pytest>=8,<9", "pytest-asyncio>=0.24,<1"]
