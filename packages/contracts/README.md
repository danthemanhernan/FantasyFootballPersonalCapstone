# Shared contracts

Reserved for versioned TypeScript/Python-compatible contracts. Keep schemas explicit and testable; keep provider fields at the adapter boundary; include valid and invalid examples. A schema is a teaching aid, not proof that a service exists.
